import { authKeys } from './auth/queryKey';
import { testKeys } from './test/queryKey';
import { voteKeys } from './vote/queryKey';

export const queryKey = {
  auth: authKeys,
  test: testKeys,
  vote: voteKeys,
};
