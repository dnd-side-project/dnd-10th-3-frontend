export type UserType = {
  id: number;
  createdAt: string;
  modifiedAt: string;
  nickname: string;
  avatar: string;
  email: string;
};

export type UserInfo = {
  userId: number;
  nickname: string;
  avatar: string;
  email: string;
};
